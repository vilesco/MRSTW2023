
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Dashboard";
}
@model System.Collections.Generic.List<AutoCar.Web.Models.UserData>
<!-- Page Title start -->
<div class="pageTitle">
    <div class="container">
        <div class="row">
            <div class="col-md-6 col-sm-6">
                <h1 class="page-heading">Admin Dashboard</h1>
            </div>
        </div>
    </div>
</div>
<!-- Page Title End -->
@Html.AntiForgeryToken()
<div class="inner-page">
    <div class="container">
        <div class="row">
            <div class="col-md-3 col-sm-4">
                <ul class="usernavdash">
                    <li><a href="@Url.Action("Dashboard", "Admin")"><i class="fa fa-tachometer" aria-hidden="true"></i> Dashboard</a></li>
                    <li class="active"><a href="@Url.Action("Users", "Admin")"><i class="fa fa-user" aria-hidden="true"></i> Users</a></li>
                    <li><a href="@Url.Action("Posts", "Admin")"><i class="fa fa-laptop" aria-hidden="true"></i> Posts</a></li>
                    <li><a href="@Url.Action("ChangePassword", "Admin")"><i class="fa fa-lock" aria-hidden="true"></i> Password Settings</a></li>
                    <li><a href="@Url.Action("UserLogout", "Dashboard")"><i class="fa fa-sign-out" aria-hidden="true"></i> Logout</a></li>
                </ul>
            </div>
            <div class="col-md-9 col-sm-8">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Full Name</th>
                            <th>Username</th>
                            <th>Email</th>
                            <th>Role</th>
                            <th></th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var user in Model)
                        {
                            <tr>
                                <td>@user.Id</td>
                                <td>@user.FullName</td>
                                <td>@user.Username</td>
                                <td>@user.Email</td>
                                <td>@user.AccessLevel</td>
                                <td>
                                    <a href="@Url.Action("EditUser", "Admin", new { userId = user.Id })" class="btn btn-primary">Edit</a>
                                </td>
                                <td>
                                    <form method="post" action="@Url.Action("DeleteUser", "Admin", new { userid = user.Id })" onsubmit="return confirm('Are you sure you want to delete this user?');">
                                        <button type="submit" class="btn btn-danger">Delete</button>
                                    </form>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
<form id="logoutForm" action="@Url.Action("Logout", "Dashboard")" method="post">
    @Html.AntiForgeryToken()
</form>